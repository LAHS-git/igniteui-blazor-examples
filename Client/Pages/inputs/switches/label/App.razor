@page "/samples/inputs/switches/label"
@page "/samples/inputs/switches-label"
@page         "/inputs/switches/label"
@page         "/inputs/switches-label"

@using IgniteUI.Blazor.Controls
@inject IIgniteUIBlazor IgniteUIBlazor

<div class="container vertical">
    <div class="wrapper" style="display: flex; align-items: center;">
        <span id="switch-label">Label</span>
        <IgbSwitch AriaLabelledby="switch-label" LabelPosition="CheckboxBaseLabelPosition.Before" />
    </div>
</div>

@code {
    protected override void OnInitialized()
    {
        IgbSwitchModule.Register(IgniteUIBlazor);

    }

}